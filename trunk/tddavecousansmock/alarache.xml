<?xml version="1.0" encoding="UTF-8" standalone="no"?><templates><template autoinsert="true" context="java" deleted="false" description="code imple pour implem" enabled="true" name="ALaRache">ChambreDao chambreDao = new ChambreDaoImpl();
		List&lt;Chambre&gt; chambres = chambreDao.findAll();

		loopchambe: for (Chambre chambre : chambres) {
			if (chambre.getCapacite() == capacite) {
				List&lt;Reservation&gt; reservations = chambre.getReservations();
				for (Reservation reservation : reservations) {
					if (nuitee.equals(reservation.getNuitee())) {
						continue loopchambe;
					}
				}
				return true;
			}
		}
		return false;</template></templates>